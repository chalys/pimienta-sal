<!DOCTYPE html>
<html>
<%- include("./partials/head") %>

  <body>
    <!-- Navbar (sit on top) -->
    <%- include("./partials/navbar") %>
      <!-- Header -->
      <%- include("./partials/header") %>
        <!-- Page content -->
        <div class="ps-content">
          <!-- About Section -->
          <div class="ps-row ps-padding-64" id="about">
            <div class="ps-col m6 ps-padding-large ps-hide-small">
              <img src="/images/principal.jpg" class="ps-round ps-image ps-opacity-min" alt="SAbores que enamoran"
                width="600" height="750" />
            </div>
            <div class="ps-col m6 ps-padding-large">
                
                <% about.forEach(ab=>{%>
                  <h1 class="ps-center"><%= ab.title %></h1>   
                  <br />
                  <p class="ps-large">
                    <%= ab.textOne%>
                    <span class="ps-tag ps-light-grey"><%= ab.textTwo%></span> <%= ab.textThree%>
                  </p>                 
                  <p class="ps-large ps-text-grey ps-hide-medium">
                    <%= ab.textFour%>
                  </p>
                <%})%>
              </h1>
            </div>
          </div>
          <hr />
          <!-- Menu Section -->
          <div class="ps-row ps-padding-64" id="menu">
            <div class="ps-col l6 ps-padding-large">
              <h1 class="ps-center">Nuentro Menu</h1>
              <% menus.forEach(menu=>{%>
                <a class="ps-link" href="/menu/detalle/<%=menu.id%>">
                  <h4>
                    <%= menu.title %>
                  </h4>
                </a>
                <p class="ps-text-grey">
                  <%= menu.description %>
                    <%= menu.price %>
                </p>
                <%})%>
                  <br/>
            </div>
            <div class="ps-col l6 ps-padding-large ps-img-menu">
              <img src="/images/menu.jpg" class="ps-round ps-image ps-opacity-min" alt="Menu" />
            </div>
          </div>
          <!-- Footer -->
          <%- include("./partials/footer") %>
        </div>
  </body>

</html>